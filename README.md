# sd-fsm-viz

Instalação:
  ```
  (Instale Node.js)
  npm install javascript-state-machine
  npm install viz.js
  npm install express
  npm install svgutils
  ```
  ou
  ```
  npm install
  ```
Uso:
  ```
  Node fsm.js table.txt input.txt
  ```
